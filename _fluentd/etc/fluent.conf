<source>
  @type forward
  port 24224
</source>

<match blog.search>
  @type elasticsearch
  host elasticsearch
  port 9200
  logstash_format true
  logstash_prefix search
  type_name searchlogs
</match>
